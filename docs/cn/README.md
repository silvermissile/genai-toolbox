# 中文文档

本目录包含 MCP Toolbox for Databases 项目的中文文档。

## 📚 文档列表

### [Ubuntu 开发完整指南](./ubuntu-development-guide.md)

适用于在 Ubuntu 24.04 LTS 系统上从零开始开发本项目的完整指南。

**适合人群**:
- Go 语言新手
- 第一次在 Ubuntu 上配置 Go 环境
- 需要将 Go 工作目录配置到数据盘的用户

**主要内容**:
- ✅ Go 语言安装（从零开始）
- ✅ Go 依赖管理机制详解（Go vs Python）
- ✅ 数据盘配置（/data 挂载点）
- ✅ 开发工具安装（golangci-lint 等）
- ✅ 项目依赖管理
- ✅ 开发工作流
- ✅ 常见问题解决
- ✅ Cursor IDE 使用技巧

**快速开始**:

```bash
# 1. 安装 Go
cd /tmp
wget https://go.dev/dl/go1.25.5.linux-amd64.tar.gz
sudo rm -rf /usr/local/go
sudo tar -C /usr/local -xzf go1.25.5.linux-amd64.tar.gz

# 2. 一键配置到数据盘
bash /data/github/genai-toolbox/docs/cn/setup-go-data-disk.sh

# 3. 使配置生效
source ~/.bashrc

# 4. 下载项目依赖
cd /data/github/genai-toolbox
go mod download
```

---

## 🛠️ 辅助工具

### 1. [setup-go-data-disk.sh](./setup-go-data-disk.sh) - 普通用户版本（推荐）

自动配置 Go 环境到 `/data` 数据盘的一键脚本（适用于普通用户）。

**适用场景**: 使用普通用户账号进行开发（**推荐**）

**功能特性**:
- 🔍 自动检查 /data 目录和磁盘空间
- 📁 创建完整的 Go 目录结构
- 💾 自动备份现有配置
- ⚙️ 配置环境变量（GOPATH、GOMODCACHE、GOCACHE）
- 🔄 可选迁移已有的 Go 缓存
- ✅ 验证配置正确性

**使用方法**:

```bash
# 使用普通用户运行（不要用 sudo）
bash /data/github/genai-toolbox/docs/cn/setup-go-data-disk.sh
```

**脚本执行步骤**:
1. 检查 /data 目录是否存在
2. 检查可用磁盘空间（建议 5GB 以上）
3. 创建 Go 工作目录结构
4. 备份现有的 shell 配置文件
5. 添加或更新 Go 环境变量
6. 可选：迁移已有的 $HOME/go 目录
7. 验证配置并提供下一步指引

---

### 2. [setup-go-data-disk-root.sh](./setup-go-data-disk-root.sh) - Root 用户版本

专为 root 用户设计的配置脚本（不推荐日常开发使用）。

**适用场景**: 
- ⚠️ 仅在必须使用 root 用户时使用
- ⚠️ 服务器环境或容器环境
- ⚠️ 出于安全考虑，不推荐日常开发

**使用方法**:

```bash
# 使用 root 用户运行
bash /data/github/genai-toolbox/docs/cn/setup-go-data-disk-root.sh
```

**安全警告**: 
- 脚本会多次提醒安全风险
- 建议创建普通用户进行开发
- 仅用于系统级配置

---

### 3. [create-dev-user.sh](./create-dev-user.sh) - 创建开发用户

帮助快速创建一个用于开发的普通用户账号。

**适用场景**: 
- 当前是 root 用户，想创建普通用户进行开发
- 新系统初始化

**功能特性**:
- 👤 创建普通用户账号
- 🔑 设置用户密码
- 🛡️ 添加到 sudo 组
- 📁 自动设置项目目录权限
- ✅ 配置完整的开发环境

**使用方法**:

```bash
# 使用 root 权限运行
bash /data/github/genai-toolbox/docs/cn/create-dev-user.sh

# 按提示输入用户名（默认: developer）
# 设置密码
# 完成后切换到新用户
su - developer
```

**创建后的步骤**:
1. 切换到新创建的用户: `su - username`
2. 运行 Go 配置脚本: `bash /data/github/genai-toolbox/docs/cn/setup-go-data-disk.sh`
3. 开始开发

---

## 🗄️ 数据源集成文档

### Apache Kyuubi 数据源

完整的 Kyuubi/Spark SQL 数据源集成，支持大数据查询场景。

- [⚡ Kyuubi 快速开始](./KYUUBI_QUICKSTART.md) - **5 分钟快速上手**
- [📖 Kyuubi 集成指南](./KYUUBI_README.md) - 快速开始、配置详解、故障排查
- [💡 Kyuubi 使用示例](./KYUUBI_EXAMPLES.md) - 20+ 实际业务场景示例
- [🔧 Kyuubi 配置示例](./kyuubi-example-tools.yaml) - 完整的 YAML 配置文件
- [📋 Kyuubi 实现总结](./KYUUBI_IMPLEMENTATION_SUMMARY.md) - 实现细节和文件清单
- [🎯 database/sql 在大数据中的必要性](./DATABASE_SQL_FOR_BIG_DATA.md) - **技术选型深度解析**
- [🏗️ Kyuubi Go 客户端技术方案](./kyuubi_go_client_design.md) - 详细的技术设计文档

**快速开始**:

```yaml
# tools.yaml
sources:
  my-kyuubi:
    kind: kyuubi
    host: kyuubi-server.example.com
    port: 10009
    username: ${KYUUBI_USER}
    password: ${KYUUBI_PASSWORD}
    authType: PLAIN

tools:
  query-data:
    kind: kyuubi-sql
    source: my-kyuubi
    description: 查询数据
    statement: SELECT * FROM table LIMIT {{.limit}}
    templateParameters:
      - name: limit
        type: integer
        required: true
```

---

## 📖 相关文档

- [项目主 README](../../README.md) - 项目整体介绍
- [开发者指南](../../DEVELOPER.md) - 详细的开发文档（英文）
- [在线文档](https://googleapis.github.io/genai-toolbox/) - 完整的项目文档网站

---

## 🆘 获取帮助

如果在使用过程中遇到问题：

1. 📖 查看 [Ubuntu 开发完整指南](./ubuntu-development-guide.md) 中的"常见问题解决"章节
2. 🐛 查看项目的 [GitHub Issues](https://github.com/googleapis/genai-toolbox/issues)
3. 💬 加入 [Discord 社区](https://discord.gg/Dmm69peqjh)
4. 📝 阅读 [Medium 博客](https://medium.com/@mcp_toolbox)

---

## 🤝 贡献

欢迎贡献中文文档！如果你发现文档中的错误或有改进建议，请：

1. Fork 本项目
2. 创建新分支进行修改
3. 提交 Pull Request

请遵循 [贡献指南](../../CONTRIBUTING.md) 中的规范。

---

**文档维护**: MCP Toolbox 社区  
**最后更新**: 2025-12-22  
**文档版本**: 1.0

